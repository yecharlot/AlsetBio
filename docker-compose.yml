services:
  alsetbio:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - alsetbio_data:/app/alset_data
    restart: unless-stopped

volumes:
  alsetbio_data:
